# CloXray plugin (LiquidWobbleMPB) — build history

Moved out of the inline `PluginBuild` comment in `LiquidShaderWobbleEffect.cs` for source clarity. Newest first.

- **438** — FIX (complete): stencil-pair reset on scene load — 437 only plugged the "update existing copy" hole; `EnsureBodyReveal`/`EnsureBodyVeil`'s CREATE path still stamped unconditionally, and on load our KKAPI CharacterReloaded handler can run BEFORE MaterialEditor restores the saved copy (ME has no ordering guarantee) → `existing==null` → CREATE stamps the stale default 4 over the saved pair-B. FIX: `OnCharacterReloaded` now DEFERS via `WobbleSceneController.DeferApply` — a coroutine that yields a frame then polls up to ~5s (`AutoBodyReveal.MERestoredFor`, the same wait pattern the wobble/BP re-attach use) for the BodyReveal MECopy to appear. Present → `ApplyForCharacterNow` ADOPTS it (overwriteExisting=false → zero stencil writes on a saved scene) and re-runs `OnBodyRevealApplied` (the runtime-only interior/cum un-hide, which ME does NOT persist). Never appears within the cap → genuinely fresh/swapped char → create. Net: a plain load NEVER writes a stencil; hotkey + fresh-char-swap still apply. Renamed ApplyForCharacter→ApplyForCharacterNow.
- **437** — FIX: stencil-pair reset on scene load. `AutoBodyReveal.ApplyForCharacter` (fires on every `CharacterReloaded`, incl. scene load) re-derived the body stencil from `womb.OrganStencil()` and `EnsureBodyReveal`/`EnsureBodyVeil` OVERWROTE an existing copy's `_StencilRef`/`_StencilBody_Plus_1`. On a non-default pair (8/12/16) the womb's `_StencilBody` may not be MaterialEditor-restored yet when the plugin reads it → it read the stale baked default 4 and clobbered the user's saved pair-B body copies back to 4 (pair A never showed it since its value IS 4). Added an `overwriteExisting` param: on-LOAD (`ApplyForCharacter`) passes **false** (ensure the copy exists but leave its stencil — the saved copy is authoritative), the HOTKEY (`ApplyAll`) passes **true** (explicit re-stamp). Mirrors how the veil already protects its XrayAlpha from a re-press. Body/penis/mesh otherwise unchanged.
- **436** — PERF: off-screen collider-scan skip. WombExpandEffect.LateUpdate's per-frame collider block (`CfgReactColliders` → `ColliderBridge.TryReadNearCanal`) now also requires `(engaged || _smr == null || _smr.isVisible)`, so an idle, non-engaged, off-screen womb skips its collider scan (scales with womb count in busy scenes). A penis-engaged womb's `engaged` is already true here so a mid-thrust bounds-cull never freezes it; the ring loop keeps smoothing so returning on-screen re-opens smoothly (no snap). No behaviour change for a single on-screen womb. (Considered + REJECTED the BPBridge.TryReadCore array-reuse from the audit: `_bp` is a persistent per-womb field read across methods, so a static reused buffer would alias every womb's shaftPos/girthPos → multi-womb corruption.)
- **435** — F1 MASTER TOGGLE + NC-EVENT PAIRING. (1) New "General/Enabled" config (default ON); OFF = the plugin freezes (per-frame drives early-out: WombExpandEffect.LateUpdate/OnPreCullCanal, LiquidShaderWobbleEffect.FeedFrame) AND all BP/NC reach-ins go inert via WombExpandEffect.EffectiveActive (= CfgEnabled && AnyActive). FREEZE semantics (no teardown): wombs hold last openness/fill; flip ON resumes live. (2) womb<->penis pairing is now FULLY EVENT-DRIVEN with NO fallback poll: Harmony postfixes on the 3 NodesConstraints methods that touch a k_f_dan_entry constraint (AddConstraint / SetConstraintEnabled toggle / RemoveConstraintAt) + CharacterReloaded + the hotkey + womb-enable each bump WombExpandEffect._repairVersion; a womb re-runs FindByEntry once per bump (the old 1s _pairTimer poll is removed). A seated womb's entry is NC-pinned so it only moves via those discrete ops -> no safety-net poll needed (per the user's no-fallbacks rule; fail-loud if a hook can't install).
- **434** — ZERO-EFFECT-WHEN-UNUSED: the BP-interop hooks (BPDanReaddGuard, PenisFKEnforcer) AND the on-load penis-FK-off coroutine are gated on WombExpandEffect.AnyActive (count of live CloXray wombs). No womb in the scene -> the Harmony patches aren't installed AND their prefix/postfix early-out, so a non-CloXray scene is left completely untouched (BP + penis FK exactly as the user posed them). Also made the shared cf_J_Vagina_root cache EVENT-DRIVEN (InvalidateVaginaRoots on CharacterReloaded / womb OnEnable; 30s safety fallback) instead of the 2s poll.
- **433** — RELEASE quiet: `_logger = null` in Awake -> ALL plugin logging is a no-op (no log-file output). Dev: comment out the `_logger = null;` line + rebuild.
- **432** — PERF: EnsureVaginaPairing ran FindObjectsOfType<Transform>() (full-scene scan) every 2s PER WOMB -> a ~2s frame stall (spiked DynamicBone breasts). Now a SHARED static cache: ONE scan/2s for all wombs, each womb measures distance to the cached cf_J_Vagina_root list. Also gated the per-frame warn-only AssertRigRigid rigidity check behind Debug Log (no per-frame bone scan / no warning spam in release).
- **431** — removed the GL debug line/cross overlay (WombExpandEffect.OnRenderObject) — it used the raw cam.projectionMatrix, which skews on DirectX so the lines/crosses swam with the camera; the GameObject sphere markers (correct Unity-pipeline projection) remain.
- **430** — DEBUG consolidated to ONE place: the F1 "Debug Log" switch. The shader tip overlay is driven by CfgDebugLog (plugin sets _ShowTipDebug via the block + feeds tip data only when on) instead of a material slider; _ShowTipDebug pulled from Material Editor; _ShowSetupPhysicsBounds moved into the boundary-box category. Off by default.
- **429** — ENTRY-DETACH withdrawal gate. BP keeps the AIMED tip up in the womb after you pull out, so the tip-detach misses it; but for a penis whose k_f_dan_entry is NOT NC-pinned to this womb's vagina the entry tracks the BASE -> withdrawal swings it OFF the canal axis (~5mm inserted -> ~65mm out). New gate `entryAttached = entryLateral <= CfgEntryDetach` (config "Penis entry off-axis limit (m)", default 0.05). ANDed into engagement. Only sideways gated (below-along-canal = normal insertion); NC-pinned entries stay on-axis so never false-close. why-string + _entryLat diag.
- **428** — WIRED M's clo_canal_entry bone. If the womb mesh ships it (M added it under cf_j_kokan, local +Y = canal axis, self-verified 0.000mm), RefreshCanalWorld OVERRIDES entrance = bone.position + axis = bone.up — exact + scale/rotation/position-NATIVE (the BakeMesh re-project double-applies lossyScale on a scaled womb). Length stays the baked scalar. Searched once (in EnsureCanal, across _smr.bones' children). ABSENT (old mesh) -> falls back to the BakeMesh walk (CANAL-BONE log says which).
- **427** — yellow tip CLAMPED to the canal. k_f_dan_end is the aimed ENDPOINT (user places it BEYOND the real tip), so 426 over-shot. Real penis tip can't go past the cervix -> clamp the shown tip's depth to [entrance..cervix]: over-aimed endpoint snaps to the cervix (= the real tip, where magenta sits), partial insertion shows actual depth. Marker-only; engagement depth was always BP visualDepth (never the endpoint).
- **426** — yellow tip marker now shows the REAL deep tip. The cm_j_dan deepest bone gets squished SHALLOW by BP (bunches near the base while the mesh extends deep), so the yellow sat low/off-axis. Now the marker uses the DEEPER (along the canal axis) of the cm_j_dan bone vs the NC-aimed k_f_dan_end (read POST-NC via new BPBridge.GetEndWorld) -> in NC-aimed scenes the yellow lands at the real deep tip. NOTE: this is the MARKER only; the engagement DEPTH/girth/ring-opening come from BP visualDepth/collider radii and never used the tip position, so the squished read never affected the animation magnitude (only the coarse engage gate, which worked).
- **425** — debug yellow TIP marker now shows for womb2 too. Its 50mm lateral gate (added to reject cross-paired tips) was hiding the marker when the paired penis's deepest bone sits >50mm off-axis (womb2's angled anal insertion ~58mm). Pairing is now locked to ONE penis by entry node so there's no cross-pairing to reject -> gate loosened to max(90mm, womb bulb radius). Animation was already correct; this is the debug marker only.
- **424** — womb2 wouldn't animate: pairing was now correct (womb2->chaM_003) but the TIP-DETACH closed it ('k_f_dan_end pulled 204mm > detach 200mm'). womb2 is an ANUS (cf_j_ana) aimed DEEP, so the tip is far from the mouth but INSIDE. Old detach used straight distance -> a deep tip read as 'pulled out'. Now DIRECTIONAL: withdrawn only if the aimed tip is BELOW the mouth (along < -detach) or far SIDEWAYS (lateral > detach); a tip deep up the canal (along +, on-axis) stays inserted.
- **423** — TIMING fix: k_f_dan_entry was read in LateUpdate = BEFORE NodesConstraints drives it, so the plugin saw the penis-rig DEFAULT entry (at the penis) not the NC-driven one (at the vagina/anus) -> green marker far from the red NC node, and the pairing matched on wrong positions. Moved the entry PAIRING (FindByEntry) AND the grabbed-entry marker read (new BPBridge.GetEntryWorld) into OnPreCullCanal (Camera.onPreCull, after all LateUpdates = post-NC), like the canal bake. LateUpdate now only reads the already-chosen penis.
- **422** — the lateral metric was RIGHT; the bug was the ABSOLUTE sticky threshold (60mm) HOLDING a 57mm off-axis penis (chaM_002) over the 26mm on-axis one (chaM_003) for womb2. Sticky is now RELATIVE: hold the current pairing only while it's within 20mm lateral of the BEST candidate; a clearly-more-on-axis penis (>20mm better) wins. Thrust nudges laterals a few mm -> no flip; wrong/stale initial pick self-corrects. Entry READ confirmed fine via the green marker (it was just the wrong penis grabbed).
- **421** — DEBUG VISUALS for the pairing (lateral metric grabbed wrong penis): (1) the cyan channel line now extends 25cm BELOW the mouth (the line the pairing tests entries against), (2) GREEN cross+sphere at the GRABBED penis's k_f_dan_entry + a green line mouth->entry, so the user can SEE which entry was chosen and whether it sits on the channel line. Reading.entryPos exposes the paired penis's entry. Metric unchanged (lateral-from-axis) pending the visual to show if the axis direction is wrong or the entries are ambiguous.
- **420** — pairing metric FIXED per user's NC screenshots: the entry node always sits BELOW the womb mouth, DOWN THE CHANNEL (NC shift + body seating), and "below" rotates with the womb. So match by LATERAL offset from the womb's CHANNEL AXIS (perpendicular distance to the axis line), at/below the mouth — not raw 3D distance to the mouth point (which mis-picked a neighbour's closer-but-off-axis entry; womb2 is tilted toward chaM_003 so chaM_003 is down-its-axis even though chaM_002 is a nearer point). STICKY now on lateral (hold while entry stays on-axis <=60mm; a thrust drags the entry ALONG the axis so lateral stays small -> no flip; re-aim moves it off-axis -> re-pair). BP-PAIR log dumps each male's lat=/alng= mm.
- **419** — STICKY entry pairing + alignment diagnostics. Hold the womb's current penis while ITS k_f_dan_entry stays attached (<=150mm of the womb mouth) so a thrust (BP drags the vagina/entry toward the penis) or a withdrawal can't flip the pairing to a neighbour; re-pair to the nearest male only when the current entry moves away (NC re-aim / toggle off -> marker jumps to the penis base). BP-PAIR log now dumps EVERY male's entry distance to the womb mouth + the current's distance + hold decision (to verify mis-alignment: in the big scene chaM_003's entry was ~70mm off both mouths while chaM_002 was ~13/59mm, and mouths sat ~60mm above the entries).
- **418** — canal cached as just the TWO endpoints (entrance + cervix), not 9 band-centres. The detection only ever uses entrance/axis/length = a straight line (BP drives the penis along a line too); the 9 walk-centres were a byproduct of finding the cervix + the old orange debug trail. RefreshCanalWorld now re-projects 2 points/frame instead of 9; removed the orange band-trail markers (cyan=entrance, magenta=cervix already mark the line).
- **417** — canal SHAPE bake is now ONE-TIME (was every 2s). The local rest-mesh canal structure is constant (reactions are blendshapes, zeroed in the bake; move/rotate/scale is a transform handled live by RefreshCanalWorld), so re-baking recomputed an identical result -> pure waste + the only recurring BakeMesh cost. EnsureCanal now retries (1s) ONLY until the first valid bake, then short-circuits forever (re-bakes only if the item reloads -> component re-inits). Pose at bake time no longer matters since RefreshCanalWorld re-projects the cached LOCAL points through the live post-IK transform each frame.
- **416** — entry-pairing re-check sped up 2s->1s so toggling a NodesConstraint on/off (which moves k_f_dan_entry to/from the womb mouth) is picked up within ~1s. The re-check reads the LIVE entry position so it reflects the constraint state automatically — no NC-plugin event hook needed.
- **415** — REWORK per user: womb<->penis pairing is now decided by the ENTRY node (k_f_dan_entry, pinned at the womb mouth by the user's NodesConstraints cf_J_Vagina_root->k_f_dan_entry) on a SLOW 2s timer, NOT per-frame. REMOVED all the per-frame matching: shaft-containment loop (411), per-frame claim registry (414), hysteresis (414), NC-aim score (414), segment-distance (408), DistSqToSegment. New BPBridge.FindByEntry(wombMouth,range)->charName picks the nearest-entry MALE; the womb caches it and per-frame calls TryReadLocked(name) which reads ONLY that one penis (no scan, no loop work for others). TryReadNear reverted to simple nearest-tip (cum slosh/debug only). Stable by construction: the entry marker doesn't move unless NC changes (re-checked every 2s).
- **414** — STABILITY for multi-womb/penis: wombs picked their penis INDEPENDENTLY with no rule against two wombs grabbing the same one -> "two yellows merge to one / womb2's yellow vanishes when penis1 moves into its column". Now (a) per-frame CLAIM registry: each penis goes to ONE womb (the one it threads most); a womb with no UNCLAIMED penis falls back to the best claimed one (two OVERLAPPING wombs on one penis still both react); (b) HYSTERESIS: each womb prefers last frame's penis (+6 score, ~3 shaft bones) so a neighbour briefly entering can't flip it; (c) the NodesConstraints-aimed k_f_dan_end now counts in the score (+3) so an NC-aimed penis wins its womb even if the shaft lags. Claim only applies to real-canal womb pairings (cum-overlay/slosh callers pass canalLen 0 -> unaffected).
- **413** — DIAGNOSTIC++: user uses NodesConstraints (penis_target->k_f_dan_end) to aim each penis at its womb, but pairing reads the cm_j_dan* SHAFT bones (mesh skeleton), which only follow if BP bends them. cand= dump now reports per male: (Nsh = shaft bones in canal, Aaim = is the NC-aimed k_f_dan_end in canal, Dmm = tip dist) -> distinguishes 'NC marker is in womb2 but the shaft/mesh is near womb1' from 'nothing here'. Requested a MINIMAL repro scene (2 wombs + 2 penises) to remove the 7-controller noise.
- **412** — DIAGNOSTIC: both wombs STILL pair chaM_002 after shaft-containment (411) -> chaM_003's shaft isn't in either womb's canal column. Added per-male candidate dump to the why= log: cand=[name(shaftInCanal,tipDistMm) ...] so we can SEE chaM_003's shaft count + tip distance vs chaM_002 and confirm whether chaM_003's BP bones thread the womb at all (user suspects the female's single BP system means the 2nd penis isn't BP-deformed into the canal).
- **411** — after the female filter (410) two SEPARATE wombs still both paired with chaM_002 (womb2 at 45mm) because chaM_003's TIP bone sits below womb2's entrance (BP squishes the deep penis' tip bone toward the base) so tip-distance picked the neighbour by a hair. Pairing PRIMARY metric is now shaft-containment: pick the penis with the MOST shaft bones inside this womb's canal column (along the axis within a 35mm lateral radius); squish-proof. Tie / no shaft data -> nearest tip to the segment (old). Should pair womb2 -> chaM_003.
- **410** — FOUND IT: BP-CACHE showed 7 controllers — on MALES *and* FEMALES (chaF_*). Females carry vestigial cm_j_dan* bones so BP exposes a phantom penis tip at the female's crotch (right where the womb sits), and wombs paired with that / the wrong male instead of the real male penis inside them -> both wombs converged on one tip. FIX: per-womb pairing (TryReadNear) now SKIPS controllers on chaF_* characters; only real male penises are candidates. Added Reading.srcName + 'pairedWith=' in the why= log to verify each womb pairs with a DIFFERENT male. Legacy global TryRead unchanged.
- **409** — DIAGNOSTIC: user confirms this spot has TWO penises (two males) one per womb, but both wombs read the IDENTICAL tip after 408 -> the plugin only SEES one of the two BP penises. Added BP-CACHE log (FindObjectsOfType(BetterPenetrationController) count + each danTip world pos + owning GameObject) on each cache refresh, gated by Debug Log, to reveal whether the 2nd male's penis is detected at all (likely BP not active / inactive object -> FindObjectsOfType skips it).
- **408** — ROOT of the 'yellow wrong' = a PAIRING bug, not just a marker: with two wombs+penises close, a womb grabbed the NEIGHBOUR's penis. BPBridge pairing picked the penis whose tip is nearest to the womb's ENTRANCE POINT, and worse, the anchor passed was EntranceWorld() (the entrance bone sits ~20cm off the detected opening). Now pairs by squared distance to the womb's real canal SEGMENT (_canalEntrance .. _canalEntrance+axis*canalLen) so each womb pairs with the penis THREADING ITS canal. TryReadNear takes (anchor, canalAxis, canalLen, range); cum-overlay/slosh callers pass a degenerate segment (= old point-distance). This fixes both the engagement (right penis drives each womb) and the yellow marker.
- **407** — yellow STILL wrong in the multi-womb scene: engaged-gating (406) wasn't enough — with overlapping/adjacent wombs the BP pairing flips frame-to-frame and a womb intermittently reads a tip from a DIFFERENT nearby penis (e.g. clo_xraywomb2 sens=1.90 engaged a tip 60mm off-axis), so yellow + the mouth->tip line shot across to the wrong canal. Now the tip marker (sphere + GL cross + line) only draws when the tip is genuinely INSIDE this womb's canal column: engaged AND tip projects onto the canal axis with lateral <50mm and along in [-50mm, canalLen+100mm]. A cross-paired/far tip is rejected.
- **406** — multi-womb scene: the YELLOW penis-tip marker (sphere + GL cross + the mouth->tip line) drew whenever _bp.found, so a womb that merely had a penis NEAR it (withdrawn / in another womb, raw=0, not engaged) still showed a tip, and as those loose pairings flipped frame-to-frame the tip + long lines jumped ("jumpy"). Now gated on _dbgEngaged (= the per-womb `engaged` flag) so the tip marker only shows for a womb actually engaged with a penis. Canal markers (cyan/magenta/orange) were already stable.
- **405** — user confirmed 404 stable (entrance no longer moves, no stationary animation). Declutter: removed the WHITE (renderer-bounds centre) + GREEN (bounds bottom) CALIBRATION markers now that post-IK marker placement is confirmed. Remaining debug markers: CYAN = opening, MAGENTA = cervix, ORANGE trail = canal path, YELLOW = penis tip (all still gated by Debug Log; full strip happens at release).
- **404** — STABILITY fix: the 403 adaptive walk read the DEFORMED mesh each frame, so penetration blendshapes moved the detected canal -> entrance jumped on penetration AND the canal length flickered, feeding back into depth/reaction so the womb ANIMATED while stationary ("purple jumps up/down on pause"). Now: (1) TryCanal bakes the REST shape (saves+zeros+restores the blendshape weights around BakeMesh, restored pre-render so no flicker) -> canal STRUCTURE is independent of the penetration animation -> no feedback loop; (2) cache the canal in LOCAL space and re-project through the live post-IK transform EVERY frame (RefreshCanalWorld) so it tracks the womb without a per-frame bake or lag.
- **403** — CALIBRATE the canal span: post-IK (402) put the markers on the womb, but the detected canal was a fixed 8.5cm CENTRED sub-segment (entrance ~2cm above the opening, top ~2cm below the cervix). TryCanal now does an ADAPTIVE local-Y climb from the opening: skips empty bands below the first hit (rest-pose slit) to find the mouth, then climbs until the cross-section opens into the uterus (ring `spread` > 30mm or > 2.4x the entrance spread = the CERVIX) instead of a fixed band list. Kept LOCAL-Y (canal runs along the mesh's +Y -> orientation-robust for rotated/anus wombs; world-Y would break those). RingCenter now returns `spread` (median tube radius). Dropped the stale CANAL-SCREEN draw-vs-detect log.
- **402** — ROOT CAUSE found via the calibration spheres: the womb's _smr.bounds (white=ctr 0.501 / green=bottom 0.383) AND the BakeMesh canal float ~15cm ABOVE where the uterus actually renders, while the penis tip (real bone) is correct. = the pose-timing gotcha: BakeMesh + _smr.transform sampled in LateUpdate catch the PRE-IK/pre-constraint rest pose; the womb renders POST-IK (pulled down). FIX: moved the authoritative canal re-bake to Camera.onPreCull (post-IK, exactly like the cum-fill feed) via OnPreCullCanal; EnsureCanal(drive) — drive=true (onPreCull) does the timed post-IK bake, drive=false (LateUpdate) only bootstraps so it never overwrites the good post-IK value with a pre-IK one. Debug markers now placed from onPreCull too. CANAL-DIAG tagged postIK=. If the post-IK entW comes out LOWER (on the visible opening), it was timing.
- **401** — the spheres landed on the SAME pixel as the old GL crosses -> the GL projection was FINE all along; the cyan "wrong place" is a DETECTION result (bake-min-Y entrance sits at the cervix/root ~Y0.42, not the outer vaginal opening ~Y0.25), NOT a draw bug. Restored the GL cross+line markers (user asked) ALONGSIDE the spheres as a cross-check. Added CALIBRATION spheres: WHITE @ true renderer-bounds centre, GREEN @ bounds bottom; and an ORANGE trail of the detected canal band-centres (entrance..top) to show which way the bake-walk ran. Goal: confirm marker space == render space, then fix TryCanal to anchor the entrance at the real opening.
- **400** — markers STILL invisible in 399 -> two changes: (1) put each marker GameObject on the WOMB MESH's layer (CreatePrimitive defaults to layer 0/Default which the Studio camera's culling mask may not render, while the womb is on its own studio-item layer that IS rendered) — the prime suspect; (2) one-shot DBG-MARKERS diagnostic log (shaderFound, womb vs marker layer+name, active, rendEnabled, material shader, vertex/colour counts, scale) so if they're STILL invisible the log pinpoints why.
- **399** — make the debug sphere markers actually VISIBLE: 398 created them but Hidden/Internal-Colored outputs (vertexColor * _Color) with an alpha blend, and a primitive sphere has NO vertex-colour channel -> D3D11 reads (0,0,0,0) -> the spheres blended to fully transparent (user saw NO channel markers). Now: bake the marker colour into the cloned sphere MESH vertex colours + force OPAQUE blend (One/Zero) so they can never blend away; _Color=white. Sizes bumped (cyan 16mm / magenta 11mm / yellow 13mm).
- **398** — FIX the debug marker drawing for good: REMOVED all GL drawing from OnRenderObject (GL.LoadProjectionMatrix got the raw camera matrix, NOT the GPU clip matrix -> on DirectX the cyan canal mouth rendered far above the womb even though its 3D point sits ON the canal, 4mm from cf_j_root). Debug markers are now REAL GameObject spheres (CreatePrimitive) positioned at the world points and rendered by Unity's own pipeline -> projection is correct by construction. CYAN sphere = canal mouth (_canalEntrance), MAGENTA = canal-axis far end (canal top), YELLOW = paired penis tip; ZTest Always so they show through the body. Managed in UpdateDebugMarkers() from LateUpdate; torn down when Debug Log is off / OnDestroy. PROVES draw-vs-detect: _canalEntrance(-0.224,0.416,-0.595) == cf_j_root(-0.225,0.413,-0.599), so the entrance was ALWAYS on the canal; only the GL draw was wrong.
- **397** — draw candidate canal anchors: BLUE = item root (transform.position, what the user grabs in Studio), PURPLE = SMR rootBone; log both in CANAL-DIAG. To test the user's idea that the canal entrance should sit at the womb pivot/beginning rather than the BakeMesh-minY detected mouth (which lands above+behind the channel for a posed/anus-oriented womb).
- **396** — FIX the debug DRAW bug: OnRenderObject GL lines now load the current camera's projection + worldToCamera matrices (GL.PushMatrix/LoadProjectionMatrix/GL.modelview) before drawing. Before, GL.Vertex used a leftover/wrong projection so the cyan/markers were offset on screen even though their 3D values were correct (proven: WorldToScreenPoint put the entrance ~180px from the penis while GL drew them far apart). DETECTION uses the 3D values directly and was never affected.
- **395** — CANAL-SCREEN: log WorldToScreenPoint of the canal entrance vs the womb bounds-centre vs the penis tip (camera-accurate) to settle whether the cyan marker projects above the womb or it's a perspective read.
- **394** — canal diag++: draw RED cross at the SMR transform pivot + orange lever-arm line to the canal entrance; log entrance computed via unit-scale TRS vs TransformPoint (catch scale/frame error over the ~1.1m lever) + dist-to-pivot + dist-to-bounds-center.
- **393** — debug GREEN wireframe BOX of the womb's true renderer bounds (alongside cyan canal entrance/axis + yellow penis tip) so the human can see whether the canal entrance is inside the womb or off it.
- **392** — CANAL-DIAG: log the raw TryCanal data per womb (baked-local mesh bbox, the local canal-mouth/top centers, the world entrance/top they get pushed to, the SMR transform pos/euler/lossy, and the renderer's true world bounds) to find why the cyan canal marker lands off the womb opening on all wombs.
- **391** — DEBUG read-markers per womb (gated by Debug Log): CYAN cross+line = the canal mouth+axis the plugin computes, YELLOW cross = the penis tip it paired with/reads, faint white line = mouth->tip. World-space GL in OnRenderObject, draws on top. Lets the human see directly if the read point sits on the visual penis or off it (diagnosing womb2 "reads off but looks in-channel").
- **390** — FIX over-reaction: the vagina-paired engagement path had NO lateral check, so a 2nd/display womb seated near ANOTHER womb's active penis engaged off BP depth alone (penisTipLat 80mm+). Now gated by _bpLateral <= max(WombBulbRadius, PenisInCanalWidth)*sens — so a penis in an adjacent womb is rejected even at default sens, and low BP_Sensitivity shuts a display womb up. Legit insertions (a bone near the axis -> small _bpLateral) + no-pose fallback (_bpLateral<0) unaffected.
- **389** — removed the global "Penis in-canal width" (CfgPenisInCanal) F1 config — superseded by the per-womb BP_Sensitivity slider; the lateral floor is now the const PenisInCanalWidth=0.06 (same as the old default, behavior unchanged).
- **388** — per-womb "BP_Sensitivity" control blendshape (penetration TRIGGER sensitivity): weight/50 -> 0=off(manual), 1.0=default(weight 50), 2x=looser; scales the canal-containment lateral gate (DeepestShaftInCanal *= _sens) + the depth-to-engage. Default (absent channel OR weight 50) is BEHAVIOR-IDENTICAL to 387. Needs M to add the inert o_uterus.BP_Sensitivity channel (baked default 50) to both wombs.
- **387** — REVERTED the bulb-wall bone-poke experiment (builds 384-386): removed the cage-bone poke (ResolveCageBones/UpdateWallPoke/ClosestOnSeg + fields), the "Wall poke max push" config, ColliderBridge.GatherCapsules, and the AssertRigRigid cage exemption. Clean womb restored; test-rig zipmod removed.
- **386** — wall poke max push now a LIVE F1 knob "Wall poke max push (m, TEST)", default 0.30m (=30cm) + 6x overlap gain so a normal collider can drive the wall that far; +updateWhenOffscreen so big bulges aren't culled.
- **385** — wall poke now EXPANDS (bulges the bulb wall radially OUTWARD under a collider) instead of denting inward — user: "only expand, it contracts". No-overshoot lerp follow so it can never go inward of rest.
- **384** — TEST: collider-driven BULB-WALL poke for M's `cage_bulb_*` test rig (`_TESTRIG` zipmod). On spawn, finds the 16 cage bones; each LateUpdate, spring-pushes them away from any placed DynamicBoneCollider (name-filtered, ColliderBridge.GatherCapsules) so the wall dents locally where poked + jiggles back. Gated on the cage bones existing -> NO-OP on the clean womb. Cage bones exempt from the fill rigidity assertion; cum not weighted to them so the liquid doesn't follow (expected for the look-test). Reversible = redeploy clean womb.
- **383** — config declutter: removed the 'Stretch Cap' and 'Max Ring Weight' cfg entries from F1 — both were hard-capped at 100 (a >100 blendshape write latches in KKPE), so they were effectively constants. Now fixed at 100 in code. No behavior change.
- **382** — mound L/R (side) lean halved (sideScale 0.5) vs fwd/back — sideways lean was too strong. fwd/back unchanged.
- **381** — mound lean (a) SIGN-CORRECTED: 380 leaned the wrong way on BOTH axes in-game (L/R + F/B reversed) -> negated the womb-local lateral so it leans TOWARD the penis; (b) sqrt() response curve so a small off-axis angle reacts fast, easing toward the 'Direction reaction' ceiling (lower that cfg to "grow lower").
- **380** — mound lean re-based on penis APPROACH DIRECTION (tipDir) instead of the tip's POSITION offset from the canal axis. Old code used (tipPos-entrance).NORMALIZED -> on an off-seated overlay the tip's constant sideways offset gave a full-strength lean that never let go ("moundleft always engaged"). Now: lateral component of tipDir (base->tip), un-normalized, so lean is PROPORTIONAL to the actual approach angle (~0 when the penis runs straight up the canal).
- **379** — V6 is MANUAL-ONLY: removed Vagina_6_entrance_open from the auto-driven RingBlendShapes (human wants the dome controlled by hand via KKPE, not the auto animation). Auto now drives 5 rings V1..V5 (Vagina_5_top = deepest = CervixWeight), same as before M's rework, just on the new names/shapes. V6 stays at the manual weight (plugin only overrides shapes it lists).
- **378** — (superseded) had V6 in the auto list.
- **377** — TIP-DETACH close: the womb also treats the penis as withdrawn when the tip marker k_f_dan_end is pulled farther than CfgTipDetach (0.20m default) from the entrance. BP's raw depth does NOT fall when you move ONLY the tip (a constraint/sphere on k_f_dan_end), so 376 kept the womb open after you pulled the tip out; this closes it, enabling sphere-driven tip animation. Inserted tip ~0.08-0.13m, pulled 0.3m+. Tunable; only gates when k_f_dan_end exists.
- **376** — BP-on-vagina is now the MAIN penis signal (not a fallback). Bone-containment ("does a dan bone thread the canal") proved unreliable: BP length-squishes a long penis so its bones bunch 130mm+ BELOW the cervix while the mesh + BP sit deep, so the womb wouldn't react though clearly inserted (confirmed across a full male swap — BP opened right, our geometry didn't). New rule: engage when raw>eps AND (womb-on-a-vagina OR bones-thread-canal); depth = BP visualDepth (position-based, holds on static poses). Containment kept ONLY as a no-regression OR (can't gate OUT). Colliders/toys keep their own geometric path. EnsureVaginaPairing re-added (2s cache).
- **375** — REVERT of the 371-374 off-seated-overlay investigation (driven by a BROKEN scene whose penis was scaled ~2x girth / 1.44x length independent of the male body, which fed bad data). Dropped: 371 aimed-tip posed fallback, 372 "Engage reach below entrance" (+CfgEngageReach), 373 vagina-pairing fallback (+EnsureVaginaPairing/_vaginaPaired/CfgAutoBodyRevealRange accessor), 374 girth de-bulge (never deployed). Engagement + girth are now IDENTICAL to build 370 (flat bulb-radius penis containment; BP-depth or real-shaft posed depth; raw tip girth).
- **370** — penis flat bulb-radius containment (367) when BP is ACTIVELY penetrating (raw>eps) but the penis runs BESIDE the narrow canal column (the womb is seated off the penis path in the screen-depth axis the user can't see) AND this womb sits ON the vagina (entrance within 'Womb-in-vagina range' of cf_J_Vagina_root, cached 2s), drive the womb from BP's own depth. Closes on withdrawal (raw->0); gated raw>eps so the 357 posed-glue 'opens while withdrawn' bug can't recur. Well-seated wombs still use the precise canal-column path. New diag src=penis-VAGINA-PAIR + a 'why=' that reports vaginaPaired/raw when off-axis.
- **372** — "Engage reach below entrance" (CfgEngageReach, default 0.05m): a STATIC/posed penis whose FREE tip sits just BELOW the canal mouth (womb seated slightly ABOVE the penis -> looks inside from the camera but ~30mm under in 3D, the off-axis offset hidden in the screen-depth axis) now opens the womb PROPORTIONALLY. Reactive zone runs [-reach .. canalLen] -> [0 .. 1]. Unified the two posed branches (real-shaft / aimed-tip) into ONE that drives from the DEEPER of the real shaft vs the free aimed tip; the 12cm dan-bone guard is kept so a k_f_dan_end glued to penis_target on a WITHDRAWN penis stays rejected (356 'opens while withdrawn' bug stays dead). Does NOT touch an actively-thrusting BP penis (branch 1, BP's own depth) or colliders.
- **371** — posed off-centre aimed-tip fallback (superseded by 372's unified branch).
- **370** — penis flat bulb-radius containment (367) instead of a depth-scaled one — consistent off-centre-womb behaviour (a penis 73mm off the placement axis but inside the womb's half-width now engages; was rejected at shallow depth). Does NOT change well-aligned scenes or a penis that's below the entrance (that's a placement/insertion issue, not containment).
- **369** — multi-collider per-ring each ring opens to the THICKEST collider reaching its depth (two toys -> a thick one + a thin one each shape their own rings). TryReadNearCanal now fills a per-ring max-radius array (every qualifying collider contributes its radius to the rings its body spans); the single deepest still sets engagement depth. Single collider = same as before (covers all opening rings uniformly). COLLIDER-PERRING diag.
- **368** — penis distance-from-tip taper BP girth profile (per-collider pos+radius) re-exposed; each ring opens to the penis part (_depth - ringDepth) back from the tip -> cervix=thin tip, entrance=thicker shaft, growing with insertion. Uses the penis's intrinsic curve + insertion depth, so the off-centre womb doesn't break it. Falls back to uniform tip girth for a toy collider / no profile. GIRTH-DIAG verifies.
- **367** — flat bulb-radius collider tolerance (was depth-scaled in 366). With the womb placed off the insertion path, the collider reads ~65mm off-axis at EVERY depth, so the depth-ramp only caught it deep (sudden pop-in mid-insertion); 'inside the womb's half-width' is the real test. lateralMax = floor. Penis path unchanged.
- **366** — depth-scaled (popped in deep only) like the penis path, so a collider inside an ANY-orientation / off-centre-placed womb counts (a fixed 45mm gate rejected a toy 54mm off the centreline of a womb the user placed off the insertion path). Orientation itself was already handled (TryCanal bakes in womb-local space -> a flipped womb gets a correctly down-pointing axis). Reverted the 0.08 cfg band-aid to 0.045.
- **365** — endpoint-depth diag centre-along (cA), capsule height (h) + canalLen — to resolve why a visually-deep collider measures reach~6mm (axis inverted for this womb placement? toy enters opposite the canal axis?). Diagnostic only.
- **364** — collider runs regardless of penis + deeper-of-two the toy-collider path now runs REGARDLESS of the penis path (was gated behind !engaged, so a barely-engaged penis blocked the toy) and the womb reacts to the DEEPER of penis vs toy. Keeps COLLIDER-DIAG. With a penis + a toy both in, the toy inserted deeper now drives the womb.
- **363** — COLLIDER-DIAG diagnostic only 361's GIRTH-DIAG proved per-ring is worthless: BP's colliders all sit below the uterus entrance, so every ring mapped to the same nearest (tip) collider every frame. Kept the accuracy win (tip girth = the glans that dilates the cervix, vs the whole-shaft average that over-counts the fat base); stripped the profile arrays, LocalGirthAt, GIRTH-DIAG.
- **361** — per-ring (dead) BP's girth is NOT uniform (per-collider radii: base/glans fatter than mid-shaft). Reading now carries the girth PROFILE (per-collider world pos+radius). Each womb ring opens to the LOCAL penis thickness at ITS canal depth (LocalGirthAt interpolates the profile), instead of one global average. GIRTH-DIAG logs the profile + per-ring localRad/scale.
- **360** — mouth lean fwd/back + left/right Driven from the penis tip's LATERAL offset (tipPos - canalEntrance, perpendicular to canal) — same direction BP uses to pull its vagina (PullKokanBones along danEndTarget-entry), so ours moves in lockstep. up/down dropped (not a lean; mounddown left alone). left/right no-op until M adds the shapes.
- **359** — (wrong) up/down wiring (girthBase/RefGirth); dropped BP's deep-press girthFactor (climbs to ~1.2 on cervix contact) from width. Log proved push raised gScale 0.76->1.04 -> rings 50->68 ('push -> max open' #3, 'squish adds expansion' #5). The press still displaces/stretches via depth; canal now holds a steady girth-matched width (also trims 'fully inserted slightly too open' #2).
- **357** — false-open-when-outside fix 356 added k_f_dan_end to the shaft scan, but it's GLUED to penis_target when aimed -> read 'deep inside' every frame -> the posed-depth fallback fired even fully withdrawn (log: 23 frames raw=0 yet eng=True via penis-TIP fallback, rings=50). Now SPLIT: aimedTipPos (k_f_dan_end) = CONTAINMENT-only (confirms a BP-registered insertion); realShaft (cm_j_dan only) = physical depth AND the posed fallback. Withdraw -> real bones leave the canal -> closed.
- **356** — added k_f_dan_end (caused the false-open) Log proved: with the penis aimed at the womb (NodesConstraint), k_f_dan_end is at canal along=57mm/lat=36mm (inside) while every cm_j_dan bone curls out below the entrance -> 355 rejected it. BP also reported raw depth=0.49, so adding k_f_dan_end as a candidate makes contained=true -> engages on BP depth. SHAFT-DIAG (per-bone along/lat) kept this round to confirm.
- **355** — whole-shaft scan (missed the aimed tip) (BPBridge.Reading.shaftPos = every cm_j_dan bone), not just the single deepest bone. After the BP-bend fix the geometric tip curls OUT of the womb (log: deepest bone y=0.300, 119mm BELOW the entrance) while the shaft still threads it (k_f_dan_end 36mm from womb centre) -> single-tip test read 'outside' -> dead reaction. DeepestShaftInCanal scans all shaft bones, keeps the deepest inside a depth-scaled womb radius (girth-narrow at entrance -> rest-mesh bulb-wide deep, tilt-independent).
- **354** — release cleanup of the 353 penis-FK cure: enforcer now acts ONLY on a male with an ACTIVE BP penis (BPBridge.HasActiveBp / danTargetsValid) — a hand-FK-posed non-BP penis is never touched; stripped the DANROT/DANFK/PenisBend-DIAG diagnostic logging (DumpDanRotations, LogDanFkState, the WaitForEndOfFrame logger).
- **353** — CURE (correct). 352's DANFK proved: dan FK nodes are ONLY 103/105/107(+119), in BoneGroup BODY (grp=0->Body=1) SHARED with the whole skeleton (bodyInGrp=all body bones) -> ActiveFK(group,false) would kill body FK, unusable. The odd-bone mystery = those are the ONLY shaft bones with an FK node; the rest are always BP-driven. Lever = per-bone TargetInfo.enable (en=1 stuck, en=0 free). CURE = clear enable + deactivate guide on JUST the dan nodes; ENFORCED by a Harmony postfix on OCIChar.ActiveFK (load re-applies saved activeFK[] AFTER our one-shot — that race is why 352 no-op'd then re-pinned).
- **352** — wrong group guess
